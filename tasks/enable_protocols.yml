---
- name: Enable SQL Server TCP protocol via registry
  ansible.windows.win_regedit:
    path: 'HKLM:\SOFTWARE\Microsoft\Microsoft SQL Server\MSSQL{{ ludus_mssql_version_map[ludus_mssql_version] }}.{{ ludus_mssql_instance_name }}\MSSQLServer\SuperSocketNetLib\Tcp'
    name: Enabled
    data: 1
    type: dword
  notify: restart mssql

- name: Enable TCP for all IPs via registry
  ansible.windows.win_regedit:
    path: 'HKLM:\SOFTWARE\Microsoft\Microsoft SQL Server\MSSQL{{ ludus_mssql_version_map[ludus_mssql_version] }}.{{ ludus_mssql_instance_name }}\MSSQLServer\SuperSocketNetLib\Tcp\IPAll'
    name: TcpPort
    data: "1433"
    type: string
  notify: restart mssql

- name: Clear dynamic ports for IPAll
  ansible.windows.win_regedit:
    path: 'HKLM:\SOFTWARE\Microsoft\Microsoft SQL Server\MSSQL{{ ludus_mssql_version_map[ludus_mssql_version] }}.{{ ludus_mssql_instance_name }}\MSSQLServer\SuperSocketNetLib\Tcp\IPAll'
    name: TcpDynamicPorts
    data: ""
    type: string
  notify: restart mssql

- name: Open Windows Firewall for MSSQL TCP 1433
  community.windows.win_firewall_rule:
    name: "MSSQL TCP 1433"
    localport: 1433
    action: allow
    direction: in
    protocol: tcp
    state: present
    enabled: true

- name: Open Windows Firewall for MSSQL UDP 1434 (SQL Browser discovery)
  community.windows.win_firewall_rule:
    name: "MSSQL UDP 1434"
    localport: 1434
    action: allow
    direction: in
    protocol: udp
    state: present
    enabled: true
  when: ludus_mssql_enable_udp_discovery | default(true) | bool
